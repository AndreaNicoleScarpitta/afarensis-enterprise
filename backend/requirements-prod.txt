# =============================================================================
# Afarensis Enterprise — Production Dependencies (slim)
# Use this on Replit/Railway/Render to avoid installing 5GB of ML libs
# =============================================================================

# Core framework
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings>=2.7.0

# Database
sqlalchemy==2.0.36
aiosqlite>=0.19.0
asyncpg>=0.29.0         # PostgreSQL async driver (used when DATABASE_URL is postgres)

# Auth & Security
bcrypt==4.0.1
python-jose[cryptography]==3.3.0
cryptography>=41.0.0

# HTTP client (PubMed, ClinicalTrials.gov, Semantic Scholar)
httpx>=0.25.0
aiohttp>=3.9.0

# Document generation (CSR, ADRG, SAR exports)
python-docx>=1.1.0

# Scientific computation (statistical engine + feasibility assessment)
numpy>=1.24.0
scipy>=1.11.0
pandas>=2.0.0

# Web scraping (external API HTML parsing)
beautifulsoup4>=4.12.0

# Network/graph analysis (citation networks, causal DAGs)
networkx>=3.0

# Visualization (TFL generation)
matplotlib>=3.7.0

# File handling
python-multipart>=0.0.6
aiofiles>=23.0.0

# Utilities
python-dateutil>=2.8.0
email-validator>=2.0.0
orjson>=3.9.0

# Observability (optional — gracefully skipped if SENTRY_DSN not set)
sentry-sdk[fastapi]>=2.0.0

# WSGI server (production)
gunicorn>=21.0.0
